package org.example.Entity;

public enum OperationType {
    EXPENSE,
    INCOME;
    public enum ExpenseCategory{
        FOOD,
        RESTAURANT,
        ENTERTAINMENT,
        MEDICINE,
        TRANSPORT,
        RENT
    }
}
